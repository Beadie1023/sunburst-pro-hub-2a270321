# Supabase / Lovable Cloud client configuration
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_PROJECT_ID=""

# Server-side only secrets are configured through the project's secret store,
# never in client-side environment variables.
